# Неделя 3: Контрольная приемка

## Автотесты

- Команда:
  - `cd backend`
  - `.\.venv\Scripts\python.exe -m pytest -q`
- Результат:
  - `12 passed`

## Проверенные сценарии

- Создание, чтение, обновление, мягкое удаление media item.
- Создание и удаление связей между media item.
- Защита от дублирующих и зеркальных связей.
- Пагинация списка (`limit`, `offset`).
- Поиск и фильтрация (`q`, `type`).
- Сортировка (`sort_by`, `order`).
- Негативные сценарии валидации (`422`, `400`, `409`).

## Вывод

- Backend-часть недели 3 закрыта:
  - каталог media
  - связи media
  - фильтрация/пагинация/сортировка
  - серверная валидация и предсказуемые ошибки
