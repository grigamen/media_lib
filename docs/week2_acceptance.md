# Неделя 2: Контрольная приемка

## Цель приемки

Проверить, что backend-аутентификация (включая 2FA) реализована и работает по основным сценариям.

## Результаты автоматических тестов

- Команда: `.\.venv\Scripts\python.exe -m pytest -q`
- Результат: `4 passed`

## Результаты контрольного сценария API

Проверен сквозной сценарий:

1. `POST /auth/register` -> `201`
2. `POST /auth/login` (без 2FA до активации) -> `200`
3. `POST /auth/refresh` -> `200`
4. `POST /auth/2fa/setup` -> `200`
5. `POST /auth/login` (после активации 2FA, с challenge) -> `200`
6. `POST /auth/2fa/verify` -> `200`

Фактический результат:

```text
{'register': 201, 'login_no_2fa': 200, 'refresh': 200, 'setup_2fa': 200, 'login_with_2fa_challenge': 200, 'verify_2fa': 200}
```

## Вывод

- Пункты недели 2 по backend auth закрыты.
- Базовая приемка пройдена: регистрация, логин, refresh и 2FA-сценарий работают корректно.
